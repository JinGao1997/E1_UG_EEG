# UG ERP Project — Project Structure & Reproducibility Guide

This document describes the directory layout and analysis workflow for
the Ultimatum Game (UG) ERP study covering Experiment 1 (E1: sequential
face-then-offer) and Experiment 2 (E2: simultaneous face+offer). It is
intended for collaborators and external researchers reproducing or
extending the analyses.

The project root is whatever directory contains `UG_ERP_Project.Rproj`;
on the development machine this resolves to `C:/Code/UG_ERP_Project/`.
R scripts find this root automatically via the `here` package; most
Python scripts auto-detect it. Two scripts rely on hardcoded absolute
paths and require one-line edits when the project moves machines; see
section 5 for which lines.

For project metadata not covered here (funding, ethics approval,
participant pool description, etc.) see the existing `ReadMe.txt` in
the project root.

## 1. Project root layout

```
UG_ERP_Project/
├── .git/                                         version control
├── .Rproj.user/                                  RStudio session state
├── .gitignore
├── .Rhistory
├── .Rprofile                                     loads renv on R startup
├── UG_ERP_Project.Rproj                          RStudio project file (here() anchor)
├── renv/                                         renv-managed R package library
├── renv.lock                                     pinned R package versions
│
├── data/                                         raw input + pipeline output
├── results/                                      statistical & visualization outputs
├── python_modules/
│   └── hu-neuro-pipeline/                        vendored MNE-Python preprocessing module
├── Old_Scripts/                                  archived legacy scripts (not run)
│
├── Pipline_UG_OfferPhase.Rmd                     EEG preprocessing                 (4.1)
├── Sta_Behaviour_RefAlday.Rmd                    behavioral statistics             (4.2)
├── Sta_EEG_OfferPhase_RefAlday.Rmd               EEG primary statistics            (4.3)
├── Sta_EEG_OfferPhase_RefTraditional.Rmd         EEG sensitivity statistics        (4.4)
├── Topo_Master_StiLocked.Rmd                     EEG topographic plotting          (4.6)
├── Visualization_Cluster_Permutation.Rmd         cluster permutation plotting      (4.7)
├── Final_vazt-Unfair_Grouplevel_InformativePr.ipynb   HDDM modeling           (see note)
│
├── ReadMe.txt                                    project metadata (read first)
├── README_Project_Structure.md                   this file
└── gitquick.txt                                  git command shortcuts
```

The two Python visualization scripts live under `results/`, not in the
project root:

```
results/
├── Visualization_Behavior_RefAlday.py            behavioral visualization          (4.5)
└── EEG/
    └── Visualization_EEG_StiLocked.py            EEG waveform & topo data export   (4.8)
```

### Note on the HDDM script

`Final_vazt-Unfair_Grouplevel_InformativePr.ipynb` performs Hierarchical
Drift Diffusion Modeling on the unfair-trial RT/choice data. The HDDM
analysis is maintained in a parallel project tree (separate environment
and dependencies). The .ipynb here is a reference copy. Anyone running
HDDM should consult that parallel project for the canonical version
and the environment specification.

## 2. Data directory layout

The data directory has two zones with different stability requirements:

- `data/00_Raw_Input/` is the source of truth. It must never be modified
  by any pipeline. Treat as read-only and back up independently.
- `data/01_*` and `data/02_*` are pipeline products. They can be deleted
  and regenerated by re-running preprocessing. Do not edit by hand.

```
data/
├── 00_Raw_Input/                       READ-ONLY raw data (manual placement)
│   ├── raw_all_cropped_E1/             E1 raw EEG files (.set or .vhdr)
│   ├── raw_all_cropped_E2/             E2 raw EEG files
│   ├── csv_all_E1/                     E1 behavioral logs (.txt)
│   └── csv_all_E2/                     E2 behavioral logs
│
├── 01_Preprocessing_Logs_E1/           regenerable
│   ├── csv_filtered/                   cleaned per-trial logs
│   └── csv_summary/                    per-participant summary rows
├── 01_Preprocessing_Logs_E2/           (parallel structure)
│
├── 02_Pipeline_Output_E1/              regenerable
│   ├── Method_Standard/                traditional baseline subtraction
│   │   ├── Stimulus_Locked/
│   │   │   ├── trials.csv              single-trial ERP table (consumed by stats)
│   │   │   ├── ave.csv                 grand-average ERP (consumed by viz)
│   │   │   ├── channel_locations.csv
│   │   │   ├── grand_ave.csv           consumed by cluster permutation viz
│   │   │   ├── clusters.csv | perm.csv MNE cluster permutation output
│   │   │   ├── epochs/                 MNE epochs files
│   │   │   └── report/                 preprocessing QC reports
│   │   └── Response_Locked/            (parallel structure)
│   ├── Method_Regression/              Alday baseline-as-covariate
│   │   ├── Stimulus_Locked/
│   │   │   ├── trials.csv              includes Baseline_<comp> columns
│   │   │   ├── ave.csv
│   │   │   ├── channel_locations.csv
│   │   │   ├── epochs/
│   │   │   └── report/
│   │   └── Response_Locked/
│   ├── Covariates/
│   │   ├── Vp0001_summaryPostRating.csv          script-generated per-subject
│   │   ├── ...
│   │   └── SVO_PID5BF_PostRating_E1.xlsx         MANUAL: integrated trait/rating table
│   └── Baseline_Raw/
│       └── Stimulus_Locked_Values/
│           └── baseline_values.csv               consumed by EEG visualization
└── 02_Pipeline_Output_E2/              (parallel structure)
```

### Naming conventions for raw input

- Participant ID is extracted from each filename via the regex `\d+`
  (first contiguous run of digits). `Vp0001.set`, `subject_1_eeg.set`,
  and `1_data.set` all resolve to ID 1. Avoid filenames where the first
  numeric run is not the participant ID (e.g., `pilot_S01_v3` collides
  with any other file whose first digit run is also 1).
- EEG and log files are paired by ID. `Vp0001.set` in
  `raw_all_cropped_E2/` is matched to `Vp0001.txt` in `csv_all_E2/`.
- BrainVision recordings consist of three files (`.vhdr`, `.eeg`,
  `.vmrk`) sharing the same base name. All three must be present in the
  same directory; the `.vhdr` references the others by relative path.

### The integrated trait/rating spreadsheet

`SVO_PID5BF_PostRating_<EXP>.xlsx` is the input for the Stage 2
individual-difference analysis. It is NOT produced by the preprocessing
script and must be prepared by hand from:

- SVO (Social Value Orientation) raw scores: `SVO_angle`, `SVO_type`
- PID-5-BF dimensions: `Negative_Affectivity`, `Detachment`,
  `Antagonism`, `Disinhibition`, `Anankastia`, `Psychoticism`
- Post-experiment rating items: `Rating_1`, `Rating_2`, `Rating_3`

The participant ID column should be the first column (any name is
accepted; the loader treats column 1 as the ID). Missing trait columns
are silently skipped by `load_covariates()` in the statistical scripts.

## 3. Results directory layout

The `results/` directory holds three independent output trees. They
serve different downstream consumers and do not overlap.

```
results/
├── Visualization_Behavior_RefAlday.py            (script lives here)
│
├── Behavior/                                     statistical output of behavior LMM/GLMM
│   └── E2_TwoStage_Bates/                        (one per experiment)
│       ├── GLMM_Rejection/                       per-trial accept/reject
│       │   ├── REPRODUCIBILITY.txt
│       │   ├── final_model_*.rds
│       │   ├── anova_apa.md
│       │   ├── posthoc_*.md
│       │   ├── Forest_*.pdf
│       │   └── ...
│       ├── LMM_RT_main/                          RT LMM, all trials
│       ├── LMM_RT_unfair/                        RT LMM, unfair trials only (HDDM-parallel)
│       ├── Tables_Manuscript/
│       │   ├── Behavior_AllAnalyses_<EXP>.md
│       │   └── Methods_paragraph_<EXP>.md
│       └── Supplementary/
│
├── EEG/                                          three independent sub-trees
│   ├── Visualization_EEG_StiLocked.py            (script lives here)
│   │
│   ├── E2_TwoStage_Bates_Alday/                  PRIMARY: Alday baseline-as-covariate LMM
│   │   ├── Stage1_TrialLevel/
│   │   │   ├── FRN/                              (one dir per ERP component)
│   │   │   │   ├── REPRODUCIBILITY.txt
│   │   │   │   ├── final_model_FRN.rds
│   │   │   │   ├── summary_text.txt
│   │   │   │   ├── anova_apa.md
│   │   │   │   ├── r2.md
│   │   │   │   ├── posthoc_*.md
│   │   │   │   ├── step4_correlation_lrt.md
│   │   │   │   ├── step5_rePCA.md
│   │   │   │   ├── STATS_*.xlsx
│   │   │   │   ├── Forest_*.pdf
│   │   │   │   └── Plot_*.pdf
│   │   │   ├── N400/
│   │   │   ├── LPP_offer/
│   │   │   ├── N170/                             E2 only
│   │   │   └── EPN/                              E2 only
│   │   ├── Stage2_BLUP_Correlations/
│   │   │   ├── STAGE2_REPRODUCIBILITY.txt
│   │   │   ├── Primary_TheoryDriven/
│   │   │   │   ├── stage2_full_results.md
│   │   │   │   └── stage2_significant_results.md
│   │   │   ├── Exploratory_PID5BF/
│   │   │   └── Exploratory_Rating/
│   │   ├── Tables_Manuscript/
│   │   │   ├── Stage1_AllComponents_<EXP>.md
│   │   │   ├── Stage1_AllComponents_<EXP>.html
│   │   │   └── Methods_paragraph_<EXP>.md
│   │   └── Supplementary/
│   │       ├── baseline_balance/                 per-component balance checks
│   │       └── *_marginal_contrasts/             p in [0.05, 0.10) post-hocs
│   │
│   ├── E2_TwoStage_Bates_Traditional/            SENSITIVITY: traditional baseline subtraction
│   │   ├── Stage1_TrialLevel/                    (parallel structure to Alday)
│   │   ├── Stage2_BLUP_Correlations/
│   │   │   └── STAGE2_DISABLED_README.md         Stage 2 deliberately not run; see file
│   │   ├── Tables_Manuscript/
│   │   └── Supplementary/
│   │
│   └── Results_<comp>_<EXP>/                     EEG visualization output (per component)
│       └── Session_<timestamp>/
│           ├── Regression_Results/
│           │   ├── channel_locations.csv
│           │   ├── topomap_data_for_R/
│           │   │   └── <comp>_topomap_data.csv   consumed by Topo_Master
│           │   ├── <comp>_reg_waveforms.csv
│           │   ├── <comp>_Regression_Unified.tif
│           │   ├── <comp>_Regression_Contrasts.tif
│           │   ├── teap2026poster/
│           │   └── topoplots_R_<comp>/           Topo_Master output
│           │       ├── raw/
│           │       ├── diff/
│           │       ├── offer_main/
│           │       ├── emotion_main/
│           │       └── time_course_grids/
│           └── Standard_Baseline/
│               ├── channel_locations.csv
│               ├── <comp>_ave_labelled.csv       consumed by Topo_Master
│               └── (parallel topoplots_R_<comp>/)
│
└── Permutation_Visualization/                    cluster permutation visualization
    └── <EXP>/<analysis_method>/                  e.g. E2/Method_Standard/
        └── *.tif/.png                            3-panel cluster figures
```

### Why three EEG sub-trees?

`results/EEG/` contains three independent trees because they are
written by different scripts for different purposes:

1. `E*_TwoStage_Bates_Alday/` and `E*_TwoStage_Bates_Traditional/` are
   produced by the R statistical scripts (sections 4.3, 4.4). They
   contain numerical results: ANOVA tables, post-hocs, model
   summaries, R-squared, BLUPs.
2. `Results_<comp>_<EXP>/Session_<ts>/` is produced by the Python EEG
   visualization script (section 4.8). It contains figures and the
   intermediate CSV files that the R topographic plotting script
   (section 4.6) consumes downstream.

The two trees serve different stages of the manuscript: numerical
inference (statistical scripts) vs. publication figures (visualization
scripts). They reference each other only at the section 4.8 to 4.6
handoff (Python EEG viz produces what the R topo script reads).

## 4. Scripts

For each script: input source, output location, path-handling style,
and any caveats.

### 4.1 Pipline_UG_OfferPhase.Rmd

EEG preprocessing master script. Wraps `hu-neuro-pipeline` (vendored
MNE-Python module) with R-side configuration and condition-list
generation.

- Toggle `experiment_version <- "E1"` or `"E2"` (line 33).
- Toggle `use_baseline_correction <- TRUE` for Method_Standard,
  `FALSE` for Method_Regression. Run twice with opposite settings to
  produce both trees.
- Inclusion criteria: `reaction > 0` AND `RT >= 300` AND `RT <= 3000` ms.
- Reads: `data/00_Raw_Input/{raw_all_cropped,csv_all}_<EXP>/`
- Writes: `data/01_Preprocessing_Logs_<EXP>/`,
  `data/02_Pipeline_Output_<EXP>/{Method_Standard,Method_Regression,Covariates,Baseline_Raw}/`
- Path style: `here()`. Position-independent.

### 4.2 Sta_Behaviour_RefAlday.Rmd

Behavioral GLMM + LMM analyses. Three models per experiment:
GLMM_Rejection (rejection probability), LMM_RT_main (all-trials RT),
LMM_RT_unfair (unfair-trials RT, parallel to HDDM).

- Toggle `experiment_version <- "E1"` or `"E2"`.
- Reads: `data/02_Pipeline_Output_<EXP>/Method_Regression/Stimulus_Locked/trials.csv`
  (also reads from Method_Standard if Method_Regression is unavailable).
- Reads: `data/02_Pipeline_Output_<EXP>/Covariates/SVO_PID5BF_PostRating_<EXP>.xlsx`
- Writes: `results/Behavior/<EXP>_TwoStage_Bates/`
- Path style: `here()`. Position-independent.

### 4.3 Sta_EEG_OfferPhase_RefAlday.Rmd

PRIMARY EEG single-trial LMM analysis using the Alday (2019)
baseline-as-covariate framework. Two-stage: Stage 1 fixed-effect
inference per component; Stage 2 BLUP-based individual-difference
correlations.

- Toggle `experiment_version`.
- Reads: `data/02_Pipeline_Output_<EXP>/Method_Regression/Stimulus_Locked/trials.csv`
  (must contain Baseline_<comp> columns).
- Reads: `data/02_Pipeline_Output_<EXP>/Covariates/SVO_PID5BF_PostRating_<EXP>.xlsx`
- Writes: `results/EEG/<EXP>_TwoStage_Bates_Alday/`
- Path style: `here()`. Position-independent.

### 4.4 Sta_EEG_OfferPhase_RefTraditional.Rmd

SENSITIVITY EEG single-trial LMM analysis using traditional baseline
subtraction (subtraction performed upstream in preprocessing).
Mirrors section 4.3 but without the baseline covariate. Stage 2 is
deliberately disabled; see `STAGE2_DISABLED_README.md` in the output
directory for rationale.

- Toggle `experiment_version`.
- Reads: `data/02_Pipeline_Output_<EXP>/Method_Standard/Stimulus_Locked/trials.csv`
- Writes: `results/EEG/<EXP>_TwoStage_Bates_Traditional/`
- Path style: `here()`. Position-independent.

### 4.5 Visualization_Behavior_RefAlday.py

Behavioral visualization script. Reads the statistical output from
section 4.2 and renders publication-ready figures (forest plots,
posthoc panels, raw-data scatter overlays) into the same directory.

- CLI flags: `--e1`, `--e2`, `--crossexp`, `--debug`.
- Reads: `results/Behavior/<EXP>_TwoStage_Bates/` (statistical output)
  and `data/02_Pipeline_Output_<EXP>/Method_Regression/Stimulus_Locked/trials.csv`
  (raw scatter data).
- Writes: figures into `results/Behavior/<EXP>_TwoStage_Bates/`.
- Path style: auto-detect project root by searching up to 6 parents
  for a directory containing `results/` or `data/`. Position-independent.
- Backward-compat: also recognizes legacy directory names
  (`Behavioral` vs `Behavior`, `_Pub_Output_Final` vs `_TwoStage_Bates`).

### 4.6 Topo_Master_StiLocked.Rmd

Downstream of section 4.8. Renders ERP topographic maps for each
component using both Regression-corrected and Standard-baseline data
exported by the EEG visualization script.

- Toggle `experiment_id <- "E1"` or `"E2"` (line 28).
- Reads: `results/EEG/Results_<comp>_<EXP>/Session_<latest>/{Regression_Results,Standard_Baseline}/`
- Writes: `results/EEG/Results_<comp>_<EXP>/Session_<latest>/<method>/topoplots_R_<comp>/`
- Path style: HARDCODED ABSOLUTE PATH at line 31:
  `root_dir <- "C:/Code/UG_ERP_Project/results/EEG"`. Reproducers must
  edit this line to match their local installation. See section 5.
- Picks the most recent Session_<timestamp> per component automatically.

### 4.7 Visualization_Cluster_Permutation.Rmd

Renders MNE-Python cluster-based permutation test results as
3-panel publication figures (effect time-course, topographic snapshot,
cluster-thresholded time-electrode plot).

- Toggle `exp_version <- "E2"`, `analysis_method <- "Method_Standard"`.
- Reads: `data/02_Pipeline_Output_<EXP>/<method>/Stimulus_Locked/{clusters.csv|perm.csv,grand_ave.csv}`
- Writes: `results/Permutation_Visualization/<EXP>/<method>/`
- Path style: HARDCODED ABSOLUTE PATH at line 230:
  `project_root <- "C:/Code/UG_ERP_Project"`. Reproducers must edit
  this line. See section 5.

### 4.8 Visualization_EEG_StiLocked.py

Upstream of section 4.6. Reads `hu-neuro-pipeline` output (epochs,
average waveforms, baseline values) and:

1. Renders publication-ready ERP waveform figures (unified panels and
   emotion contrasts) directly to TIFF.
2. Exports topographic data as CSV files consumed by section 4.6 to
   render the actual topographic maps.

- Toggle `EXPERIMENT_VERSION = 'E1'` or `'E2'` (line 55).
- Toggle `ANALYSIS_METHOD` to `'Regression'`, `'Standard'`, `'All'`, or `'QC'`.
- Reads: `data/02_Pipeline_Output_<EXP>/Method_Regression/<comp>/`,
  `data/02_Pipeline_Output_<EXP>/Method_Standard/<comp>/`,
  `data/02_Pipeline_Output_<EXP>/Baseline_Raw/Stimulus_Locked_Values/baseline_values.csv`.
- Writes: `<script_dir>/Results_<comp>_<EXP>/Session_<timestamp>/{Regression_Results,Standard_Baseline}/`.
  With the script located at `results/EEG/Visualization_EEG_StiLocked.py`,
  this resolves to `results/EEG/Results_<comp>_<EXP>/...`.
- Path style: auto-detect project root. The detection fingerprint
  (`data/02_Pipeline_Output`, no version suffix) currently does not
  match the actual versioned directory layout, so resolution falls
  through to a `cwd` check; running the script from the project root
  ensures correct path resolution.

## 5. Reproducibility caveat: hardcoded paths

Two scripts contain hardcoded absolute paths that must be edited when
the project is moved or copied to a new machine:

| Script | Line | Current value | Edit to |
|--------|------|---------------|---------|
| `Topo_Master_StiLocked.Rmd` | 31 | `"C:/Code/UG_ERP_Project/results/EEG"` | path to your project's `results/EEG` |
| `Visualization_Cluster_Permutation.Rmd` | 230 | `"C:/Code/UG_ERP_Project"` | path to your project root |

All other scripts (Pipline, Sta_*, Visualization_Behavior_RefAlday.py,
Visualization_EEG_StiLocked.py) auto-detect their working directory
and require no edits when relocating.

## 6. Reproducibility checklist

To reproduce the full analysis from raw data:

1. Place raw EEG and behavioral logs in `data/00_Raw_Input/<...>_<EXP>/`
   (see section 2 for naming conventions). Verify pairing:

   ```r
   library(here); library(stringr)
   exp <- "E2"
   eeg_ids <- as.integer(str_extract(
     basename(list.files(here("data", "00_Raw_Input", paste0("raw_all_cropped_", exp)),
                         pattern = "\\.(set|vhdr)$", ignore.case = TRUE)), "\\d+"))
   log_ids <- as.integer(str_extract(
     basename(list.files(here("data", "00_Raw_Input", paste0("csv_all_", exp)),
                         pattern = "\\.txt$")), "\\d+"))
   identical(sort(eeg_ids), sort(log_ids))   # must be TRUE
   ```

2. Place the integrated trait/rating spreadsheet at
   `data/02_Pipeline_Output_<EXP>/Covariates/SVO_PID5BF_PostRating_<EXP>.xlsx`.
   This requires `02_Pipeline_Output_<EXP>/` to exist; create the empty
   directory by hand or run preprocessing once first.

3. If reproducing on a new machine, edit the two hardcoded paths
   listed in section 5.

4. Run preprocessing (4.1) twice with opposite `use_baseline_correction`
   settings to populate both `Method_Standard/` and `Method_Regression/`.

5. Run statistical scripts (4.2, 4.3, 4.4) for each experiment. Each
   writes a `REPRODUCIBILITY.txt` recording R version, package
   versions, model formulas, inferential method, and Bates 5-step
   diagnostics. A `Methods_paragraph_<EXP>.md` keeps the manuscript
   Methods text in sync with the actual code path.

6. Run visualization scripts (4.5, 4.7, 4.8). Run the topographic
   script (4.6) AFTER the EEG visualization script (4.8) has populated
   `Results_<comp>_<EXP>/Session_<>/`.

## 7. Backup strategy

| Path | Backup priority | Reason |
|------|-----------------|--------|
| `data/00_Raw_Input/` | Critical, off-site | Irrecoverable; comes from the experiment |
| `SVO_PID5BF_PostRating_<EXP>.xlsx` | Critical, off-site | Manual integration; loss requires re-entry |
| Analysis scripts (`*.Rmd`, `*.py`) | Critical, version-controlled | Encode the analysis pipeline |
| `data/01_*`, `data/02_*` | Optional | Regenerable from raw data + scripts |
| `results/` | Optional | Regenerable; commit key outputs to git for record |

A common operating-system safeguard is to make `data/00_Raw_Input/`
read-only at the filesystem level (e.g., `chmod -R a-w` on Unix; the
"Read-only" attribute on Windows folders). Pipeline scripts only read
from this directory, so the read-only flag does not interfere.

## 8. Software environment

R packages are managed via `renv` (see `renv.lock`). Run
`renv::restore()` on a fresh checkout to install the pinned versions.
Each `REPRODUCIBILITY.txt` produced by statistical scripts records
the actual versions used during the run.

Python dependencies: `hu-neuro-pipeline` is vendored under
`python_modules/`. The scripts resolve it via `reticulate` against the
conda environment `r-reticulate` (configured in
`Pipline_UG_OfferPhase.Rmd`, section "Python Environment Initialization").
The two Python visualization scripts use standalone Python (not
reticulate) and require: `pandas`, `numpy`, `matplotlib`, `scipy`,
`mne`, `pathlib`, plus their transitive dependencies.

## 9. Known issues and future cleanup

The following items are not blocking but are documented for awareness:

- Two scripts use hardcoded absolute paths (section 5). Migrating them
  to `here()` (R) or `Path(__file__).parents[N]` (Python) would make
  the project fully position-independent without per-machine edits.
- `Visualization_EEG_StiLocked.py` line 166 searches for a
  `data/02_Pipeline_Output` fingerprint that does not match the
  actual versioned directory name (`02_Pipeline_Output_E1`,
  `_E2`). Detection currently falls through to a `cwd` check; running
  from the project root ensures correct resolution. A one-line fix
  would be to search for `data/02_Pipeline_Output_*` via `glob`.
- Visualization scripts are split between the project root
  (`Topo_Master_StiLocked.Rmd`, `Visualization_Cluster_Permutation.Rmd`)
  and `results/` subdirectories
  (`Visualization_Behavior_RefAlday.py`, `Visualization_EEG_StiLocked.py`).
  Consolidating them under a `scripts/visualization/` subdirectory
  would simplify discovery, but would require careful path-handling
  audits and is best deferred until after manuscript submission.